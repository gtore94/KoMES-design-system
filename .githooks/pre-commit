#!/bin/sh
# KoMES — 전역 이름 충돌 검사 (pre-commit)
# 빌드 도구가 없어 모든 *.jsx가 전역 스코프를 공유하므로,
# 같은 이름의 top-level function/const가 여러 파일에 있으면 커밋을 막는다.
python3 check-collisions.py || {
  echo "" >&2
  echo "커밋 중단: 위 전역 이름 충돌을 해결한 뒤 다시 커밋하세요." >&2
  echo "(검사를 건너뛰려면: git commit --no-verify)" >&2
  exit 1
}

# KoMES 기능 명세

한의원 EMR **KoMES** UI 프로토타입의 기능·동작 명세. 화면 구조는 [README.md](README.md), 본 문서는 **화면만으로 드러나지 않는 동작 규칙** 위주로 기술합니다.

> **목업 한계** — 모든 데이터는 `*Data.jsx`의 시드값이며, 저장·삭제·내보내기 등은 실제로 데이터를 변경하지 않고 **토스트 피드백**만 표시합니다(드릴다운·필터·테마 등 화면 내 상태는 실제 동작). 백엔드·인증·청구 전송은 미구현.

---

## 1. 공통 동작

### 라우팅
- 단일 페이지 앱. `index.html`의 `App`이 `nav` 상태로 화면을 분기(`renderMain`).
- `nav`·`screen`은 `localStorage`(`komes_nav`, `komes_screen`)에 저장되어 새로고침 후 유지.
- 미구현 메뉴는 "준비 중" 플레이스홀더 표시.

### 테마
- 색상(Jade/Navy/Pink)·명암(라이트/다크/시스템)은 `<html>`의 `[data-color]`/`[data-theme]`로 적용, `localStorage` 유지. "시스템"은 OS 설정 변화를 실시간 반영.
- 화면 밀도·글자 크기·모션 줄이기도 `[data-*]` 어트리뷰트로 적용.

### 토스트 피드백
- 전역 함수 `showToast(msg, {variant, icon, duration})`, `toastProgress(시작, 완료, ms)`(시작→완료 2단계). `<ClinicToaster />`가 앱 루트에 1회 마운트되어 모든 화면에서 우하단에 표시.
- 적용: 모든 CSV·엑셀·내보내기·다운로드(진행→완료), 인쇄·전송·저장·삭제 등의 액션 버튼.

### 권한 (병원 관리)
- 역할 `원장`/`데스크`에 따라 섹션이 **편집 / 읽기 전용 / 잠금**으로 차등(`permFor`).
- 상단 "목업 전용" 토글로 데스크 화면을 미리볼 수 있음(프로덕션 제외 표기).

---

## 2. 진료

### 진료 현황 (`patients`)
- 당일 접수·대기·진료·수납 현황 보드. 환자 선택 시 진료 차트로 이동.
- 신규 환자 등록 모달(`NewPatientModal`).

### 진료 차트 (`chart`)
- 환자 차트(SOAP), 액팅 오더 패널, 자동차보험 바.
- "처방"으로 진입 시 처방 화면(`PrescriptionScreen`)으로 전환, 뒤로가기로 복귀.

### 환자 관리 (`registry`)
- 전체 환자 등록부·검색. 내보내기(토스트), 신규 등록(모달).

### 예약 관리 (`schedule`)
- 주간 예약 캘린더.

### 수납 (`payment`)
- 결제·환불 처리.

### 서식 발급 (`forms`)
- 진단서·상해진단서·의뢰서·확인서·세부내역서 등 A4 미리보기.
- 임시 저장 / PDF 저장(진행→완료) / 환자에게 전송 / 인쇄·발급 — 토스트.

---

## 3. 청구

### 보험 청구 (`claim`)
- 건강보험·한약·자보 청구 목록 및 제출 흐름(`ClaimList`·`ClaimSubmissionFlow`).

### 수금 관리 (`collect`)
- 미수금 환자 관리. 일괄 연락 / CSV 내보내기 / 독촉 캠페인 — 토스트.
- 경영 대시보드의 "미수금" 지표·알림에서 진입 가능.

---

## 4. 처방 / 비품

### 처방 관리 (`prescriptions`)
- 처방 대기열(상태별). 엑셀(진행→완료) / 출력 묶음 — 토스트. 상세 패널에서 출력·재처방.

### 약재 재고 (`inventory`) · 소모품 (`supplies`)
- 입출고·조정, 부족 시 발주 초안 생성(`onCreateDrafts`)으로 발주 화면 인계.
- 추천 설정 모달(`RecommendationSettingsModal`), 엑셀 내보내기 — 토스트.

### 발주 이력 (`orders`)
- 발주 관리·초안 작성(`PurchaseOrderDraftScreen`). 엑셀·인쇄 — 토스트.

---

## 5. 분석

### 경영 대시보드 (`stats`)
- 상단에서 레이아웃 변형 토글: **Brief**(임원) / **Dense**(밀집) / **Story**(스토리).
- 기간 선택·전월(MoM)/전년(YoY) 비교 토글·상단 알림 배너 표시 옵션(`localStorage` 유지).
- **지역별 환자 분포**: 지도(타일맵 choropleth) ↔ 막대 토글. 색 농도 = 환자 비중. 구(시군구) 클릭 시 해당 구의 **동별 분포로 드릴다운**, 백 버튼으로 복귀.
- **상단 알림 배너** CTA: "검토"→보험 청구, "수금 관리"→수금 관리 화면으로 이동.

### AI 분석 (`ai`)
- 대시보드 데이터(`DASH`)를 재가공한 **분석 리포트**: AI 요약 → 핵심 지표 → 매출 흐름 → AI 인사이트 → 강점/리스크 → 권장 액션. "리포트 재생성" 버튼(목업).

---

## 6. 병원 관리 (`clinic`)

좌측 서브탭 + 우측 섹션. 섹션은 그룹(기본/운영/법적 정보/재무/조직/브랜딩/시스템)으로 묶임. 활성 알림이 있으면 상단 **알림 센터** 버튼에 빨간 점.

### 알림 센터 (`AlertCenterModal`)
- 우측 드로어. 탭: **자동 규칙**(원장만 편집) / **히스토리** / **처리 로그**.
- 기본 진입 시 히스토리 탭은 **활성 필터**로 시작(활성 알림 우선 노출). 처리 로그 CSV 내보내기 — 토스트.

### 섹션별 동작

| 섹션 | 동작 |
|------|------|
| 한의원 기본 정보 | 편집 모달(프리필) → 저장 토스트 |
| 진료 시간·휴진일 | 진료시간 편집 모달(요일별 운영/휴진·점심, 월–금 일괄 적용, 휴진일 추가) |
| 진료실·베드 | 공간 추가 모달 |
| 장비 관리 | KPI 요약 + 목록, 장비 등록 모달, CSV |
| 치료재 관리 | 보험 치료재 카탈로그(코드·명칭·상한금액·판매자·적용기간·선별급여·사용여부), 검색, 행 수정/삭제, 등록 모달, CSV |
| 보험 임의처방 | 코드·처방명·단가·비고 목록, 검색, 등록/수정 모달(약재 조합 빌더: 판매자·약재·수량 스테퍼) |
| 처방 사전 | 처방명·출전·태그 목록, 행 펼침 시 약재 구성(칩)·메모, 등록/수정 모달(처방내용 textarea) |
| 기타 비급여 | 분류 탭 필터(투약/물리/시술/검사/기타/제증명/자동차보험), 검색, 코드·오더명·단가·VAT·구분·보고대상·과세·수정/삭제 |
| 보험 수가 | 구분 드롭다운+검색, 코드·명칭·단가·제약사·적용기간(조회형). 단가 색: 만료=빨강/유효=파랑. 준용수가 등록 모달 |
| 보험약 사용설정 | 2단 — 좌(제약사별 카탈로그, 추가) / 우(사용 목록, 코드 자동 생성·제거) |
| 상용 혈자리 설정 | 5부위(상지/하지/두경/흉복/요배) 체크 목록, 알파벳순 정렬, 부위별 선택 수, 저장 |
| 첩약 시범사업 설정 | 탕전실 정보 + 좌(한약재 목록)/우(등록 한약재: 원산지·구매가, 취급제외 빨강) |
| 첩약 시범사업 사용설정 | 3단 — 좌(질환별 카탈로그) → 중(사용 처방, 선택) → 우(약재 구성 편집·저장) |
| 진료 과목·수가표 | EDI 코드·단가·급여구분, 항목 추가, CSV |
| 면허·자격 | 자격 추가 모달 |
| 보험 연동 | 지금 동기화(진행→완료 토스트) |
| 결제·세금 | 편집 모달 |
| 분원 관리 | 분원 추가 모달 |
| 직원 관리 | `StaffManagementScreen` — 근무표·내보내기 토스트, 직원 등록 모달 |
| 로고·서식 브랜딩 | 로고 업로드 모달(파일 선택 UI) |
| 백업·데이터 | 백업 다운로드 / 지금 백업(진행→완료 토스트) |

---

## 7. 차트 설정 (`settings`)
- 탭: **외관**(모드·색상 카드 선택, 화면 밀도·글자 크기·모션) / **단축키**(키캡 목록). 변경 즉시 적용·저장.
